import { motion } from "framer-motion";


export default function Projects() {
  return (
    <section
      id="projects"
      className="scroll-mt-24 md:scroll-mt-28 py-24 md:py-32 border-t border-secondary/60 bg-secondary h-full text-accent"
    >

      <motion.h2 className="text-accent text-7xl font-semibold">Proyectos</motion.h2>

    </section>
  );
}
